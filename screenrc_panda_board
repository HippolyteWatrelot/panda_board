deflogin on

vbell on
vbell_msg "   Wuff  ----  Wuff!!  "

defscrollback 1024

bind ^k
#bind L
bind ^\
# Make them better
bind \\ quit
bind K kill
bind I login on
bind O login off
bind } history

termcapinfo vt100 dl=5\E[M

hardstatus off

termcapinfo xterm*|rxvt*|kterm*|Eterm* hs:ts=\E]0;:fs=\007:ds=\E]0;\007

hardstatus string "%h%? users: %u%?"

termcapinfo xterm*|linux*|rxvt*|Eterm* OP

termcapinfo xterm 'is=\E[r\E[m\E[2J\E[H\E[?7h\E[?1;4;6l'


defnonblock 5

## To be applied when Franka core is active

#screen -t boxes /bin/bash --rcfile .bashrc_panda_board
#screen -t panda_cart_info /bin/bash --rcfile .bashrc_cart
screen -t panda_control /bin/bash --rcfile .bashrc_control
screen -t panda_buttons /bin/bash --rcfile .bashrc_buttons
screen -t panda_ee /bin/bash --rcfile .bashrc_ee_traj
screen -t panda_joint /bin/bash --rcfile .bashrc_joint_traj
screen -t panda_joint_execution /bin/bash --rcfile .bashrc_execute
